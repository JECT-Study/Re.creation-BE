spring.application.name: core-api

spring:
  config:
    import:
      - db-core.yml
      - logging.yml
  web.resources.add-mappings: false

prefix:
  image-prefix: https://creationre.s3.ap-northeast-2.amazonaws.com/

default-image:
  game-thumbnail-url: default/default_game_thumbnail.png


---
spring.config.activate.on-profile: local

spring:
  config:
    import:
      - optional:classpath:secret.yml
